# R2f8ae - protected multiplayer read-ahead; semantic review unfinished

This is preserved partial research, not a reviewed mechanic package and not REVIEW_REQUIRED. R2f8ad remains the latest completed semantic subsection at270 mechanics/996 paths,40/40 custom DamageTypes USED, zero promoted. Static research only; no runtime, L2, Stage, production or Phase6/7 changes.

Save reason: the actually enforced weekly limit reached80% used/20% remaining. No reset credit was redeemed. This is not the retired artificial five-hour stop rule. Finish saving, validate, push/live-verify and stop.

## Evidence already acquired

The partial-evidence/twilightforest-multiplayer bundle preserves exact TF source witnesses and specification, raw Minecraft1.21.1 witnesses/specification, exact NeoForge21.1.244 witnesses/specification, and a whole outer-JAR multiplayer symbol/resource census. The manifest pins every file. Existing native boss/spawner/config/attachment witnesses remain protected and reusable; the bundle does not duplicate them into accepted mechanics.

Reading aids can be reconstructed by selected_reference.collect(spec,source_aids=True) for the two selected-reference specifications. Raw vanilla-reference.prepare reconstructs mapped source aids. Mapped source from NeoForge248 is a reading aid only; exact raw/244 bytecode remains authority. Do not treat partially read methods as accepted or infer semantics from source-aid absence.

## Source observations to finish verifying and package

- Actual EntityEvents.setup registers FinalizeSpawnEvent health adjustment, LivingDamageEvent.Post participation and LivingDeathEvent group advancement. Config snapshot in protected R2f8ac is multiplayerFightAdjuster=NONE. Enum flags distinguish NONE, MORE_LOOT, MORE_HEALTH, MORE_LOOT_AND_HEALTH; config is worldRestart. Do not enable or change config.
- Health callback: recipient type must be in multiplayer_inclusive_entities and health flag enabled. Query ServerPlayer entities in recipient bounding box inflated32/10/32, alive and noncreative/nonspectator. Count>1 plus MAX_HEALTH instance adds permanent twilightforest:group_health_boost ADD_VALUE of difficulty coefficient*(count-1): Easy20, Normal40, Hard60, otherwise0. Callback has no setHealth/heal, current-HP check or duplicate-modifier guard. Native attribute persistence, native range limits and duplicate-ID behavior require final source guards.
- Exact EventHooks.finalizeMobSpawn posts event before invoking Mob.finalizeSpawn; cancellation skips finalizeSpawn, not necessarily entity addition. Native base Mob.finalizeSpawn adjusts follow range/handedness, not HP. Native LivingEntity.onAttributeUpdated clamps current HP only when above new maximum. Native AttributeInstance.addModifier rejects duplicate ID; addPermanentModifier records persistence.
- Protected native boss spawners provide real delivery: base and Lich invoke EventHooks.finalizeMobSpawn then add entity. Naga's own finalizeSpawn fills getMaxHealth only when non-Easy and its difficulty_health_boost was absent; this is a separate boss-specific branch. Lich and Minoshroom finalizeSpawn source aids do not add HP. Finish exact admission/order comparison and avoid claiming all boosted bosses spawn with fully filled added HP. Do not redo accepted boss mechanics.
- Actual tag includes Naga, Lich, Minoshroom, Hydra, Ur-Ghast, Alpha Yeti, Snow Queen and plateau_boss. Knight Phantom is absent. Resource census contains Knight Phantom loot using multiplayer_rolls, but participation callback creates data only for tagged recipients. Establish native reachability/default behavior; do not fabricate attachment or retag it.
- Participation callback gets/creates attachment for tagged recipient on Post even if causing entity null. With nonnull source.getEntity, maybeAddQualifiedPlayer accepts ServerPlayer and list.contains dedup; no explicit positive final damage, player proximity, game-mode or config check in this callback. Direct projectile versus causing player must stay separate; no tame-owner traversal is present in that method. Exact native Post admission and equality semantics still need final guards.
- Attachment stores an in-memory List<ServerPlayer>; registration builder has no serializer. On noncanceled death, hasData triggers group advancement; this callback does not recheck tag/config. No participant clear or current-range check in the inspected attachment. HurtBossTrigger evaluates native player and hurt-entity predicates; it is not unconditional advancement completion.
- Six actual hurt_boss advancement resources: progress_naga, progress_lich, progress_hydra, progress_ur_ghast, progress_yeti, progress_glacier. Their was_in_fight criteria are alternatives alongside existing kills/items; later advancement prior-progression requirements remain. Preserve exact JSON requirements.
- MultiplayerBasedNumberProvider uses defaultRolls plus one independently sampled max(0,perPlayerFloat) for each participant after first; absent config/context/data falls back to default. MultiplayerBasedAdditionLootFunction samples value.getInt ONCE then multiplies by(n-1), adds count and clamps0..maxStackSize; extra is not clamped nonnegative. Actual registered ID is multiplayer_addition (config prose names multiplayer_multiplier); use code/resources.
- Native Minoshroom axe loot uses uniform[-2,1] addition, so negative additions can reduce/remove the stack; do not describe all branches as extra positive loot. Exact UniformGenerator/getInt native distribution and stack limit should be finalized before numerical probability claims. Lich/SnowQueen extra-roll providers use uniform[-2,1] floats with max0, a different path.
- Knight loot wraps multiplayer rolls inside LootingEnchantNumberProvider and native binomial provider. Looting provider source adds native attacking LivingEntity's enchantment level to base. Its source was read but has not yet been added to this partial exact TF witness set; pin it before relying on it in accepted contracts.
- The remaining EntityEvents enderman wrapper was only read ahead: it delegates canUse then rejects Twilight dimension type, and tick delegates. It is not semantically closed or included in this multiplayer evidence bundle. Structure spawning, remaining worldgen traps/spawners, nested ASM and global exclusions remain ordinary unfinished work.

## Exact next task

Resume the partial multiplayer review from the saved specifications/census. Finish exact native health/HP ordering, participant/post/death admission, permanent modifier and transient participant persistence, real boss-spawner paths, tag exclusions and actual loot/advancement consumers. Add source guards and actual caller/resource routing, then assemble reviewed mechanic packages/delivery paths with one primary classification each; do not count these notes as accepted mechanics. Validate + five tooling tests + diff check, commit, push and verify live SHA equality.

Then continue remaining EntityEvents callbacks and Lich worldgen trap/spawner paths, remaining nested ASM, compatibility attribution and global source exclusions. Protect R2f8 remaining-content completion, final dedup/promotion to Twilight COMPLETE, and only then begin IceAndFireCE beta15 while actual usage is healthy. No runtime boss matrix, L2, Stage, production, Phase6/7 or balance changes.
